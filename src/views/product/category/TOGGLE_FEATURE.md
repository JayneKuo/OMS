# Category Selection Toggle Feature

## 🎯 功能说明

每一级分类现在支持**切换选中状态**，点击已选中的分类可以取消选中。

## 🔄 交互逻辑

### 基本规则
```
点击未选中的分类 → 选中该分类
点击已选中的分类 → 取消选中
```

### 级联效果
取消选中或切换选中时，会自动清除后续级别的选中状态：

```
取消 L1 选中 → 清除 L2、L3、L4
取消 L2 选中 → 清除 L3、L4
取消 L3 选中 → 清除 L4
取消 L4 选中 → 无级联影响
```

## 📊 使用场景

### 场景 1：浏览不同一级分类

#### 操作流程
```
1. 点击 "Bags" → 选中 Bags，显示其下的 L2
   ┌─────────┬─────────┬─────────┬─────────┐
   │ ■ Bags  │ □ Women │    ←    │    ←    │
   │ □ Clothes│ □ Men   │         │         │
   └─────────┴─────────┴─────────┴─────────┘

2. 点击 "Bags" (再次点击) → 取消选中，L2 面板清空
   ┌─────────┬─────────┬─────────┬─────────┐
   │ □ Bags  │    ←    │    ←    │    ←    │
   │ □ Clothes│ Select  │         │         │
   └─────────┴─────────┴─────────┴─────────┘

3. 点击 "Clothes" → 选中 Clothes，显示其下的 L2
   ┌─────────┬─────────┬─────────┬─────────┐
   │ □ Bags  │ □ Tops  │    ←    │    ←    │
   │ ■ Clothes│ □ Pants │         │         │
   └─────────┴─────────┴─────────┴─────────┘
```

### 场景 2：返回上一级查看其他分类

#### 操作流程
```
1. 已选中：Bags > Women Bags > Handbags
   ┌─────────┬─────────┬─────────┬─────────┐
   │ ■ Bags  │ ■ Women │ ■ Handbag│ □ Leather│
   │ □ Clothes│ □ Men   │ □ Clutch │ □ Canvas │
   └─────────┴─────────┴─────────┴─────────┘

2. 想查看 "Clutch" 而不是 "Handbags"
   点击 "Women Bags" (取消选中 L2) → L3 和 L4 清空
   ┌─────────┬─────────┬─────────┬─────────┐
   │ ■ Bags  │ □ Women │    ←    │    ←    │
   │ □ Clothes│ □ Men   │         │         │
   └─────────┴─────────┴─────────┴─────────┘

3. 点击 "Men Bags" → 显示 Men Bags 下的 L3
   ┌─────────┬─────────┬─────────┬─────────┐
   │ ■ Bags  │ ■ Men   │ □ Wallets│   ←    │
   │ □ Clothes│ □ Women │ □ Belts  │         │
   └─────────┴─────────┴─────────┴─────────┘
```

### 场景 3：清空所有选中状态

#### 操作流程
```
当前状态：Bags > Women Bags > Handbags > Leather
   ┌─────────┬─────────┬─────────┬─────────┐
   │ ■ Bags  │ ■ Women │ ■ Handbag│ ■ Leather│
   │ □ Clothes│ □ Men   │ □ Clutch │         │
   └─────────┴─────────┴─────────┴─────────┘

点击 "Bags" (取消选中 L1) → 清空所有
   ┌─────────┬─────────┬─────────┬─────────┐
   │ □ Bags  │    ←    │    ←    │    ←    │
   │ □ Clothes│ Select  │         │         │
   └─────────┴─────────┴─────────┴─────────┘

状态：回到初始状态，无任何选中
```

## 🎨 视觉反馈

### 未选中状态
```
┌──────────────────────┐
│ □ Bags               │ ← 白色背景
│   10 sub →           │   正常边框
└──────────────────────┘
```

### 选中状态
```
┌──────────────────────┐
│ ■ Bags               │ ← 蓝色背景
│   10 sub →           │   蓝色边框
└──────────────────────┘   粗体文字
```

### hover效果（无论是否选中）
```
┌──────────────────────┐
│ Bags                 │ ← 浅灰背景
│   10 sub →           │   鼠标指针
└──────────────────────┘
```

## 💡 最佳实践

### 推荐用法

#### 1. 浏览同级分类
```
选中 → 查看子级 → 取消选中 → 选择其他同级
```

#### 2. 快速返回
```
无需通过面包屑返回
直接点击当前级别取消选中即可
```

#### 3. 清空选择
```
想从头开始？
点击 L1 取消选中，回到初始状态
```

#### 4. 添加分类时的状态
```
✅ 有选中 → Add 按钮默认创建子级
✅ 无选中 → Add 按钮默认创建一级分类
```

## ⚙️ 技术实现

### 切换逻辑
```typescript
const selectCategory = (category: LocalCategory, level: number) => {
  // 检查是否已选中
  if (selectedL1.value?.id === category.id) {
    // 已选中 → 取消选中
    selectedL1.value = null
  } else {
    // 未选中 → 选中
    selectedL1.value = category
  }
  
  // 清除后续级别的选中状态
  selectedL2.value = null
  selectedL3.value = null
  selectedL4.value = null
}
```

### 状态判断
```typescript
const isSelected = (category: LocalCategory, level: number) => {
  switch (level) {
    case 1: return selectedL1.value?.id === category.id
    case 2: return selectedL2.value?.id === category.id
    case 3: return selectedL3.value?.id === category.id
    case 4: return selectedL4.value?.id === category.id
    default: return false
  }
}
```

## 🎯 使用效果对比

### 之前（无法取消选中）
```
问题：
1. 选错了必须选择其他项才能切换
2. 无法回到"无选中"状态
3. 想查看同级其他项必须先选择它
4. 面包屑是唯一的返回方式
```

### 现在（支持切换）
```
改进：
1. ✅ 点击即可取消选中
2. ✅ 可以回到"无选中"状态
3. ✅ 取消选中后可自由选择其他项
4. ✅ 直接点击当前级别即可返回
5. ✅ 更灵活的导航方式
```

## 📋 完整交互示例

### 示例：浏览分类并添加新分类

```
步骤 1：初始状态
┌─────────┬─────────┬─────────┬─────────┐
│ □ Bags  │    ←    │    ←    │    ←    │
│ □ Clothes│ Select  │         │         │
│ □ Shoes │  L1     │         │         │
└─────────┴─────────┴─────────┴─────────┘
[Add Category] → 默认创建 L1

步骤 2：点击 Bags
┌─────────┬─────────┬─────────┬─────────┐
│ ■ Bags  │ □ Women │    ←    │    ←    │
│ □ Clothes│ □ Men   │ Select  │         │
│ □ Shoes │ □ Kids  │  L2     │         │
└─────────┴─────────┴─────────┴─────────┘
[Add Category] → 默认创建 Bags 的子级

步骤 3：点击 Women Bags
┌─────────┬─────────┬─────────┬─────────┐
│ ■ Bags  │ ■ Women │ □ Handbag│   ←    │
│ □ Clothes│ □ Men   │ □ Clutch │ Select │
│ □ Shoes │ □ Kids  │ □ Purse  │  L3    │
└─────────┴─────────┴─────────┴─────────┘
[Add Category] → 默认创建 Women Bags 的子级

步骤 4：想查看 Men Bags，点击 Women Bags (取消选中)
┌─────────┬─────────┬─────────┬─────────┐
│ ■ Bags  │ □ Women │    ←    │    ←    │
│ □ Clothes│ □ Men   │ Select  │         │
│ □ Shoes │ □ Kids  │  L2     │         │
└─────────┴─────────┴─────────┴─────────┘

步骤 5：点击 Men Bags
┌─────────┬─────────┬─────────┬─────────┐
│ ■ Bags  │ ■ Men   │ □ Wallets│   ←    │
│ □ Clothes│ □ Women │ □ Belts  │ Select │
│ □ Shoes │ □ Kids  │         │  L3    │
└─────────┴─────────┴─────────┴─────────┘
[Add Category] → 默认创建 Men Bags 的子级

步骤 6：想创建其他一级分类，点击 Bags (取消选中)
┌─────────┬─────────┬─────────┬─────────┐
│ □ Bags  │    ←    │    ←    │    ←    │
│ □ Clothes│ Select  │         │         │
│ □ Shoes │  L1     │         │         │
└─────────┴─────────┴─────────┴─────────┘
[Add Category] → 默认创建 L1
```

## ✅ 优势总结

| 功能 | 效果 |
|------|------|
| **快速切换** | 点击即可在选中/未选中间切换 |
| **灵活导航** | 无需依赖面包屑返回 |
| **清空状态** | 可以回到初始无选中状态 |
| **智能添加** | 根据选中状态智能默认类型 |
| **直观操作** | 点击当前项即可取消，符合直觉 |
| **减少误操作** | 可以随时取消错误的选择 |

## 🎓 用户习惯培养

### 新用户指引
```
1. 点击分类 → 查看子级
2. 再次点击 → 取消选中
3. 选择其他 → 切换分类
4. 点击 Add → 基于当前选中创建
```

### 快捷操作
```
- 单击选中
- 再次单击取消
- 无需使用面包屑返回
- 直接点击当前级别即可
```

## 📝 总结

切换选中功能让分类浏览变得更加灵活：

✅ **更自由** - 随时取消选中，重新选择  
✅ **更快速** - 直接点击当前级别返回  
✅ **更直观** - 点击切换，符合用户直觉  
✅ **更智能** - 添加分类时自动适应选中状态  

**核心理念**：让用户完全掌控选中状态，提供最灵活的导航体验！

